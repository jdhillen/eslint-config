import createConfig from '@jdhillen/eslint-config';

// Zero config - auto-detects Angular and TypeScript
// Note: For Angular-specific rules, consider using @angular-eslint
export default await createConfig();
